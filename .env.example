# Helius API key (required)
HELIUS_API_KEY=your_helius_api_key_here

# Filter thresholds (configurable)
MIN_MARKET_CAP_USD=10000
MIN_LIQUIDITY_USD=5000

# Fetch settings
OVERLAP_MINUTES=30
MAX_RETRIES_NO_POOL=3

# Cron schedule (default: every 8 hours)
CRON_SCHEDULE=0 */8 * * *

# Server
PORT=3001
